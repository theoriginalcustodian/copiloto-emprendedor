# Mapa de pantallas Odobi ↔ código real (web + mobile)

**Fecha:** 2026-09-16 · **Medido contra:** `main` @ `b67dc7c9` (worktree limpio) · **Fuente del diseño:** `Prototipo frontend/odobi-ui/mapa-pantallas/index.html` (48 pantallas) renderizadas por `prototipo/index.html?ver=<clave>`.

Lectura pura, sin cambios de código. Actualiza y **amplía** los diffs del 08/09 (`2026-09-08-diff-pantalla-por-pantalla-prototipo-vs-app{,-mobile}.md`), que comparaban 14 mockups; este compara las **48 pantallas del mapa**. Desde el 08/09 el único cambio de frontend en `main` fue #507 (quita el scrim de grabación en web).

**Método:** 6 auditores en paralelo (uno por dominio, 8 pantallas c/u), cada veredicto con `path:línea` leído; AUSENTE sólo tras grep amplio. Verificación cruzada manual de 6 citas al azar (DEFAULT_TAB web, capas mobile, CUIT «Cambiar» web+mobile, Pausar/Reanudar mobile, ausencia de mic en Gastos): 6/6 coinciden.

**Veredictos:** ✅ implementado (función y elementos esenciales) · 🟡 parcial (existe, faltan elementos esenciales) · 🔴 ausente · se juzga función, no color/tipografía (decisión abierta aparte). **DA** = depende de una decisión de diseño abierta (ver contrato 2026-09-16, Parte 2).

## Resumen

| | ✅ Implementado | 🟡 Parcial | 🔴 Ausente |
|---|---|---|---|
| **Web** | 14 | 23 | 11 |
| **Mobile** | 14 | 24 | 10 |

**Ausentes en las dos plataformas (10):** `splash` Splash · `entrada` Entrada · `card` La card en la función · `reveal` El reveal · `onb-promesa` La promesa · `onb-cumplida` La promesa cumplida · `consent` Just-in-time consent · `caida` Conexión caída · `limite` El límite · `vacio-visto` Calma, ya conocida.

**Donde web y mobile difieren (7):** `bloqueado` (web 🟡 / mobile ✅) · `bi-refresh` (web 🟡 / mobile ✅) · `feedback` (web 🔴 / mobile 🟡) · `hitl` (web ✅ / mobile 🟡) · `ingresos` (web ✅ / mobile 🟡) · `clientes` (web ✅ / mobile 🟡) · `apps` (web 🟡 / mobile ✅).

## Cómo abre la app (2)

| Pantalla (`?ver=`) | Web | Mobile | Qué falta | Evidencia web | Evidencia mobile |
|---|---|---|---|---|---|
| **Splash** `splash` | 🔴 | 🔴 | No existe en ningún lado el mecanismo de 4 formas circulares creciendo y contrayéndose hacia la O (6,8s) que describe la nota del mapa. Web muestra un placeholder de texto; mobile usa el splash nativo default de Expo, sin el guión de marca. Confirmable en device pero el código fuente no contiene el mecanismo. | apps/copiloto-web/src/App.tsx:39-46 — cuando `status === 'checking'` sólo renderiza `<p>Odobi — cargando tu copiloto…</p>` en texto plano. Grep de `blob\|scallop\|lockup\|monograma\|splash` en `apps/copiloto-web/src` no encuentra ningún componente de animación. | apps/mobile/app.json:30 sólo declara el plugin nativo `expo-splash-screen` (imagen estática de Expo). Búsqueda de archivos `*splash*`/`*entrada*`/`*reveal*` bajo `apps/mobile/src` no devuelve resultados; no hay componente propio. |
| **Entrada** `entrada` | 🔴 | 🔴 | Falta la animación del isotipo 'dibujándose' que termina en el signo estático de la app (idéntico al de Mi día/header), distinta del splash de primer ingreso. Hoy ambas plataformas usan el mismo spinner/placeholder genérico para cualquier arranque. | Mismo bloque de App.tsx:39-46: no hay distinción entre primer ingreso y arranques 2..n — siempre el mismo texto de carga, sin animación del isotipo dibujándose. | No hay componente `Entrada`/animación de arcos en `apps/mobile/src`; `app/_layout.tsx` sólo muestra `ActivityIndicator` mientras cargan fuentes (líneas iniciales del archivo), no el sello SVG del prototipo (`#e-ext`/`#e-int`/`#e-o1`/`#e-o2`, `Prototipo frontend/odobi-ui/prototipo/index.html:2348-2355`). |

## Las tres capas (3)

| Pantalla (`?ver=`) | Web | Mobile | Qué falta | Evidencia web | Evidencia mobile |
|---|---|---|---|---|---|
| **Mi día** `/` | 🟡 | 🟡 | Falta la portada de negocio (caja $286.000, entró/salió/por cobrar, delta) y la identidad visual de dos bloques (negro detrás / card blanca). Más grave: en ninguna plataforma 'Mi día' es hoy la pantalla de aterrizaje — el frente real es el chat, no coincide con el armazón de 3 capas del prototipo (escritorio detrás · Mi día por defecto · chat adelante). **DA:** armazón capas vs pestañas — `Prototipo frontend/odobi-ui/mockups/10-arranque/DECISIONES.md` fila 'Tabbar': la decisión de qué capa va al frente (Mi día vs chat) y si hay tabbar 'queda como decisión abierta de Martin + David, no cerrada acá'. | apps/copiloto-web/src/modules/midia/MidiaScreen.tsx:50-214 implementa el tablero de 3 solapas (para_hoy/haciendo/hecha) del detector + `PanelCalendario` (CAL1) aparte, pero no hay portada financiera (caja, entró/salió/por cobrar, delta vs mes) ni el bloque negro/card blanca del diseño. Además NO es la pantalla por defecto: `apps/copiloto-web/src/shell/AppShell.tsx:30` fija `DEFAULT_TAB: TabKey = 'chat'`; Mi día vive en el tab `midia`, no al frente. | apps/mobile/src/modules/midia/PantallaMiDia.tsx:1-60 es el mismo tablero de 3 solapas (mismo comentario 'Reemplaza al Kanban de 4 columnas retirado 2026-07-23'), sin portada financiera. Tampoco es la capa por defecto: `apps/mobile/src/shell/PantallaPrincipal.tsx:149-167` monta `EscritorioFunciones` como fondo y `ChatView` como frente del panel deslizable; Mi día es una ruta modal aparte (`apps/mobile/app/midia.tsx`) abierta desde un tile, no una capa del swipe. |
| **Funciones** `esc` | ✅ | ✅ | La función central (grid de accesos + actividad reciente) está; el conteo/orden difiere del diseño (9 vs 6 tiles, sin la fusión Contabilidad+Inteligencia que el mockup asume ya resuelta). No hay bloque negro 'ABAJO' descrito en la nota del mapa (profundidad de capas) — no verificable sin device/captura. **DA:** fusión Contabilidad+Inteligencia — el mockup 10-arranque da por unificadas ambas funciones desde el 20/08 para llegar a 6 tiles, pero el código de ambas apps las mantiene como funciones separadas (`contabilidad` e `inteligencia` en `TILES`). | apps/copiloto-web/src/modules/escritorio/EscritorioScreen.tsx:48-58,91-149 — grid de tiles + 'Actividad reciente' debajo, mismo mecanismo del diseño, pero con 9 tiles en 3×3 (incluye `ajustes`, `contabilidad`, `midia`) en vez de las 6 en grilla 3×2 por frecuencia que fija el prototipo. | apps/mobile/src/modules/escritorio/EscritorioFunciones.tsx:1-80 — mismo grid (fondo del panel deslizable) + actividad reciente, 9 `TILES` en grid de 2 filas con scroll horizontal (docstring líneas 12-19), no las 6 fijas en 3×2. |
| **Chat** `chat` | 🟡 | 🟡 | Falta el rodillo vertical de un ejemplo por vez (~4s, WCAG 2.2.2, `prefers-reduced-motion`) que enseña por imitación — hoy ambas plataformas muestran un párrafo fijo. El resto del chat (envío, HITL, tarjetas propuestas) sí está construido en otros archivos del mismo módulo. **DA:** armazón capas vs pestañas — en ambas apps el chat es hoy la pantalla de aterrizaje real (default tab web, capa frontal mobile), lo que no coincide con el prototipo donde el chat es la capa más adelante, alcanzada por gesto, no el destino inicial. | apps/copiloto-web/src/modules/chat/ChatScreen.tsx:8-9 define `WELCOME_TEXT` estático; `MessageList.tsx:159` lo pinta como párrafo fijo (`emptyHint`). Grep de `rodillo\|rotan\|carousel\|giran\|ejemplos` en el módulo chat no encuentra ninguna implementación de rotación. | apps/mobile/src/modules/chat/ListaMensajes.tsx:29,236-247 — mismo patrón: texto de bienvenida estático + título '¿En qué te ayudo?', sin rodillo de ejemplos que giran (grep sin resultados en el módulo). |

## Estados que se superponen (29)

| Pantalla (`?ver=`) | Web | Mobile | Qué falta | Evidencia web | Evidencia mobile |
|---|---|---|---|---|---|
| **Tablero** `tablero` | 🟡 | 🟡 | Faltan los fchips de categoría (Todo/Cobros/ARCA/Presupuestos/Tuyas), el banner de alerta crítica separado del listado (ej. certificado ARCA), el contador '3 para hoy · 1 en curso · 1 crítico', la acción contextual por tarjeta (verbo específico como 'Reclamar el pago'/'Renovarlo', reemplazada por botones genéricos Empezar/Terminé/Borrar) y la nota final sobre qué se cierra solo vs. lo anotado a mano. | apps/copiloto-web/src/modules/midia/MidiaScreen.tsx:36-48,50-213: 3 solapas Para hoy/Haciendo/Hechas con tap-to-expand (L197-208, L272-301) y avanzar/borrar funcional (L110-132). Sin chips de categoría, sin banner de alerta crítica ni contador de estado; confirmado por ausencia total en apps/copiloto-web/src/modules/midia/midia.css (grep 'chip\|alerta\|critic\|segb\|estado-pag' sin resultados). | apps/mobile/src/modules/midia/PantallaMiDia.tsx:75-120 (solapas), 122-257 (pantalla), 312-406 (fila con swipe Empezar/Terminé/Borrar vía ReanimatedSwipeable). Misma estructura de 3 solapas que web; mismas ausencias. |
| **Agenda** `agenda` | 🟡 | 🟡 | No existe una pantalla 'Agenda' propia (ni ruta ni tile en el escritorio): sólo un panel de sólo-lectura embebido en Mi día que muestra ÚNICAMENTE los eventos de HOY (decisión de arquitectura CAL1 §3, ya cerrada, no abierta). Faltan las secciones 'Mañana', 'Vencen esta semana', 'Sin hora' como bloques propios, el botón 'Nuevo evento' y la vista de franja horaria (rejilla con horas). | apps/copiloto-web/src/modules/midia/MidiaScreen.tsx:142,216-250 (PanelCalendario, sólo lectura, embebido arriba del tablero). packages/core/src/api/miDia.ts:206-259 (leerCalendario/CAL1 §3: sólo eventos de HOY, panel aparte del Kanban). | apps/mobile/src/modules/midia/PantallaMiDia.tsx:204,259-310 (PanelCalendario idéntico a web). Grep amplio de 'Agenda'/'calendar' en apps/mobile/src y apps/copiloto-web/src no encontró ninguna pantalla ni tile dedicado con ese nombre (sólo este panel embebido). |
| **Detalle** `detalle` | ✅ | ✅ | Nada crítico: el comportamiento (expandir en el lugar en vez de un sheet aparte) coincide con lo que el propio prototipo declara como el reemplazo vigente. | apps/copiloto-web/src/modules/midia/MidiaScreen.tsx:203 (onPress alterna `expandida`), 272-301 (fila expande in-place con detalle cliente/monto/fecha). Coincide con el prototipo (index.html:3311-3315), donde `?ver=detalle` es alias que abre el tablero con la primera fila ya expandida — reemplazó al sheet. | apps/mobile/src/modules/midia/PantallaMiDia.tsx:245 (mismo toggle `expandida`), 312-406 (fila con `numberOfLines` condicional + detalle). Misma implementación in-place que web. |
| **Grabando** `grabando` | 🟡 | 🟡 | Ninguna de las dos plataformas implementa 'deslizar a la izquierda cancela' (mockups/03-home-conversacional/DECISIONES.md:95,108, uno de los 3 gestos que el repo define). Web al menos descarta con un release <350ms (MicButton.tsx:212-214); mobile NO tiene ningún camino de cancelar sin fijar primero — soltar sin fijar SIEMPRE envía (ChatView.tsx:155). | apps/copiloto-web/src/modules/chat/MicButton.tsx:183-224 y RecordingOverlay.tsx:28-71: hold-to-record dentro del composer, sin scrim (confirmado por el commit 81bceaf9 #507 y el comentario de MicButton.tsx:273-276). Sólo trackea el eje vertical (startYRef, línea 194) para fijar; no hay eje horizontal. | apps/mobile/src/modules/chat/BotonVoz.tsx:193-207 (Gesture.Pan con onUpdate que sólo lee translationY) + ChatView.tsx:188-207 (overlay sin modal, onda flotante). Sin scrim, coincide con la revisión 24/08. |
| **Audio bloqueado** `bloqueado` | 🟡 | ✅ | Web no tiene Pausar/Reanudar, uno de los 'tres controles que define el repo' (Pausar · Enviar · Eliminar, mockups/03-home-conversacional/DECISIONES.md:95,108,133); su botón 'Cancelar' conflacia lo que el prototipo separa en Eliminar. Mobile cubre los tres controles pero sin ícono/aria de trash explícito estilo prototipo (no verificado visualmente en device). | apps/copiloto-web/src/modules/chat/RecordingOverlay.tsx:41-62: estado `locked` sólo renderiza dos botones, Cancelar y Enviar (flecha). | apps/mobile/src/modules/chat/ControlesFlotantes.tsx:30-44: `fase==='grabando'` muestra Pausar, `fase==='pausado'` muestra Reanudar, más Enviar (BotonPrimario) y Eliminar (BotonDescartar) — los tres controles. |
| **La card en la función** `card` | 🔴 | 🔴 | El mecanismo central de esta pantalla — dictar DESDE Gastos/Ingresos/Presupuestos/Clientes y que la card aterrice ahí mismo sin pasar por el chat — no existe en ninguna plataforma: el mic sólo vive en el composer del chat (y en Soporte). Es el mismo hallazgo que ya marcó la auditoría 08/09 para `11-voz-contextual` en ambos documentos; sigue vigente sin cambios de código desde entonces. | apps/copiloto-web/src/modules/gastos/GastosScreen.tsx:1-9 no importa MicButton/Composer (sólo FormularioGasto manual, línea 6); grep de MicButton en modules/ da un único resultado, modules/chat/MicButton.tsx. | apps/mobile/src/modules/gastos no matchea BotonVoz/useVozComando (grep sin resultados); TarjetaGastoPropuesto.tsx vive únicamente en modules/chat, usado desde ListaMensajes.tsx. |
| **Lo dictado, en el chat** `vozchat` | 🟡 | 🟡 | Ninguna de las dos cards muestra el `ctx-chip` 'Por voz · duración' que el prototipo usa para marcar el origen del mensaje (grep de 'Por voz'/'ctx-chip' sin resultados en ambos módulos); el mecanismo esencial (propuesta editable, nada se guarda al enviar) sí está. Como Mi día no tiene mic en ninguna plataforma, TODO dictado pasa siempre por esta ruta — no hay diferenciación con la ruta 'card'. | apps/copiloto-web/src/modules/chat/TarjetaGastoPropuesto.tsx:76-105: card editable en el chat con aviso 'Esto entendí. Revisalo y tocá Guardar — todavía no lo anoté' y Guardar/Cancelar; no persiste hasta confirmar (guardarResolucion, línea 92-100). | apps/mobile/src/modules/chat/TarjetaGastoPropuesto.tsx:55-88: mismo patrón (TarjetaPropuestaShell + FormularioGasto), mismo aviso de 'todavía no lo anoté'. |
| **Sin ventas todavía** `bi-vacio` | ✅ | ✅ | Ninguna diferencia funcional relevante encontrada; el copy y el patrón (texto, sin ilustración, acotado a la sección) coinciden con el prototipo en ambas plataformas. | apps/copiloto-web/src/modules/inteligencia/InteligenciaScreen.tsx:256-276: card 'Mejores clientes' con `mejoresClientes.length === 0` mostrando 'Todavía no hay clientes con ventas registradas.' sin ilustración, acotado a la sección (línea 258-261). | apps/mobile/src/modules/inteligencia/PantallaInteligencia.tsx:218-223: mismo texto y mismo patrón (sección, sin ilustración). |
| **Tirar para actualizar** `bi-refresh` | 🟡 | ✅ | Web sustituyó el gesto por un botón explícito (decisión declarada en el propio código), perdiendo la progresión de estados textuales que el mockup exhibe (Tirá/Soltá/Actualizando/'Al día · recién'). Mobile cubre la FUNCIÓN (gesto de arrastre + feedback visual nativo) pero con el spinner estándar de RN, sin esos mismos tres textos — no verificable sin correr el device si el spinner nativo alcanza el estándar WCAG 1.4.1 que el mockup resuelve con texto. | InteligenciaScreen.tsx:47-50,112-121 — comentario propio: 'RefreshControl (gesto táctil) → botón Actualizar explícito'. No hay gesto de arrastre ni progresión de texto 'Tirá para actualizar'/'Soltá para actualizar'/'Actualizando…'. | PantallaInteligencia.tsx:150-158 — `RefreshControl` nativo de React Native sobre el `ScrollFormulario`, con `refrescando`/`onRefresh` reales (mismo patrón que gastos/ingresos/clientes). |
| **El reveal** `reveal` | 🔴 | 🔴 | No existe la pantalla de aterrizaje del splash con el LOCKUP completo, la leyenda 'se dice o-DO-bi' y el botón que la reproduce (lexicón/SSML). El login entra directo, sin ese paso de marca. | Grep de `lockup\|wordmark\|monograma\|o-DO-bi\|pronunci` en `apps/copiloto-web/src` no devuelve ningún componente de marca ni botón de pronunciación; `LoginScreen`/`SignupScreen` no tienen paso de aterrizaje del splash. | Mismo grep sobre `apps/mobile/src` sin resultados; `apps/mobile/src/modules/auth/PantallaLogin.tsx` no tiene pantalla de reveal ni botón de pronunciación. |
| **La promesa** `onb-promesa` | 🔴 | 🔴 | Falta el hilo de chat HITL que pide los 2 permisos (Mercado Pago + Google) enmarcados como 'dos minutos', con el alcance dicho antes del permiso. Hoy ambas apps ofrecen una pantalla de catálogo completo (N servicios) sin ese marco narrativo ni la promesa explícita. | Grep de 'dos minutos'/'facturado sin cobrar'/'primero que salta'/'ya veo tu negocio' en `apps/copiloto-web`, `apps/mobile` y `apps/copiloto` (backend) sin resultados. Lo más cercano es `App.tsx:31-35,58` que tras signup navega a la pestaña `connections` (grid completo del catálogo, `ConnectionsScreen.tsx:59-121`), no al hilo conversacional de 2 permisos ('Tus servicios' / chip '2 permisos'). | `apps/mobile/app/apps.tsx` y `apps/mobile/src/modules/auth/` no tienen ningún paso de onboarding entre login y `PantallaPrincipal`; el patrón de conexiones (`PantallaApps`) es el mismo grid completo que web, no el guión conversacional 'Son dos minutos'. |
| **La promesa cumplida** `onb-cumplida` | 🔴 | 🔴 | Falta el mensaje-recibo que cierra el primer minuto con un dato real de negocio no sabido ('$147.000 facturados sin cobrar') como prueba de la promesa. No hay ningún trigger de 'primer insight post-conexión' en el código de ninguna plataforma. | Grep de '$147.000'/'facturados sin cobrar'/'ya veo tu negocio' en todo `apps/copiloto-web` sin resultados; no hay ningún recibo/hilo que dispare un primer insight de negocio tras conectar servicios. | Mismo grep sobre `apps/mobile` sin resultados; no existe flujo equivalente. |
| **Just-in-time consent** `consent` | 🔴 | 🔴 | No existe el sheet in-context (alcance por permiso, botones Conectar/Ahora no, hilo visible detrás) en ninguna plataforma — sólo un mensaje de texto que manda al usuario a navegar manualmente a Conexiones, sin explicar el alcance ni ofrecer conectar en un toque. Es el mismo gap que 02-conexiones ya señalaba el 08/09 ('falta... el sheet de consentimiento just-in-time en el chat'), sin cambios de código desde entonces. | Grep amplio ('consent','just.in.time','jit', case-insensitive) sobre apps/copiloto-web/src: sin resultados. El único mecanismo real es texto plano del dispatcher backend compartido, apps/copiloto/dispatcher_emprendedor.py:279-283: 'Para eso necesito que conectes... Andá a Conexiones, conectalo y volvé a pedírmelo'. | Mismo grep amplio sobre apps/mobile/src: único match es useSession.ts (no relacionado, autenticación). Mismo backend/dispatcher compartido que web. |
| **Conexión caída** `caida` | 🔴 | 🔴 | Falta por completo el patrón de 'la portada admite estar incompleta': no hay bloque de ingresos con delta que ocultar, ni tarjeta de alerta inyectada en el detector cuando cae una conexión (ej. Mercado Pago). Lo más cercano (badge 'Reconectar' en Conexiones) es otra pantalla, otro patrón visual, y explícitamente no está cableado a una señal real. | Grep amplio ('delta', 'Mercado Pago', 'reconectar/desconectado', 'sin conexión') en todo apps/copiloto-web/src y apps/mobile/src no encontró ninguna variante de la portada de Mi día admitiendo datos incompletos. No existe siquiera el concepto de 'portada' con cifra de ingresos + delta: el home mobile (apps/mobile/src/shell/PantallaPrincipal.tsx:52-80) es escritorio de 6 tiles + chat, sin bloque de ingresos. | Mismo resultado; lo único tangencialmente relacionado es apps/copiloto-web/src/modules/connections/ServiceCard.tsx:6,34-36,77 (estado visual 'reconnect'/badge 'RECONECTAR'), que vive en la pantalla de Conexiones/Apps (no en Mi día) y está documentado como NO conectado a ninguna señal real del backend todavía (L22-32: 'HOY nada en el catálogo lo dispara'). |
| **Pregunta libre** `preg` | 🟡 | 🟡 | El mecanismo esencial (preguntar y recibir respuesta en el hilo sin gate) está en ambas plataformas, pero vive en un mini-chat separado y sincrónico atado a la pantalla Inteligencia, no unificado con el chat principal/durable como en el prototipo (un solo hilo con dividers de día). Tampoco se puede verificar 'no gasta acciones' porque no existe ningún concepto de consumo de acciones en el backend (ver fila `limite`) — no hay con qué contrastar. **DA:** fusión Contabilidad+Inteligencia (afecta si Inteligencia sigue con su propio mini-chat o se funde con el principal) | apps/copiloto-web/src/modules/inteligencia/ChatInteligencia.tsx:32-169: Q&A libre contra `preguntarInteligencia`, sin ningún gate/HITL, respuesta inline. El chat principal también soporta texto libre sin HITL vía Bubble (apps/copiloto-web/src/modules/chat/MessageList.tsx:206,254-262). | apps/mobile/src/modules/inteligencia/ChatInteligencia.tsx (equivalente confirmado por grep, mismo patrón que web). |
| **El comprobante** `recibo` | 🟡 | 🟡 | El principio de fondo (el resultado queda en el mismo hilo que lo produjo, no en otra pantalla) SÍ se cumple. Pero no existe el componente `REC()` del prototipo: falta el ícono de check con `aria-live`, el título en negrita + líneas secundarias de detalle, y el link accionable opcional (Ver/Detalle/Copiar) — hoy es sólo una línea de texto plano. | apps/copiloto-web/src/modules/chat/TarjetaFacturaPropuesta.tsx:59-64: al confirmar, se reemplaza por un tile de texto plano ('Factura emitida.'), sin ícono de check, sin líneas secundarias ni link accionable. apps/copiloto-web/src/modules/chat/MessageList.tsx:252-263: la confirmación HITL genérica (no factura) resuelve a un mensaje de asistente en burbuja simple, no a un componente de recibo dedicado. | apps/mobile/src/modules/chat/TarjetaPropuestaShell.tsx:69-90 (`TarjetaPropuestaTerminal`): mismo patrón, texto plano con color/peso según tono ('exito'/'tenue'), sin ícono ni link. |
| **Facturar por voz** `fact-voz` | ✅ | ✅ | No se verificó en runtime que el LLM pregunte estrictamente UN dato por vez (CUIT, luego tipo) como en el prototipo — depende del prompt del agente en tiempo real, no del código de UI; requiere prueba end-to-end con voz real. | apps/copiloto-web/src/modules/chat/TarjetaFacturaPropuesta.tsx:57,87-119 deriva `faltantes` del backend y muestra 'Esto entendí, pero falta completar algo antes de emitirla.' El backend calcula qué falta (apps/copiloto/afip_rules.py, apps/copiloto/afip_factura_workflow.py, grep 'faltantes' en apps/copiloto/tool_catalog.py) y el diálogo de a una pregunta por vez ocurre como texto plano de chat (ya soportado por MessageList.tsx/useChat.ts). | apps/mobile/src/modules/chat/TarjetaFacturaPropuesta.tsx:51,84-91 — mismo patrón 1:1 que web ('Esto entendí, pero falta completar algo antes de emitirla.'), mismo backend compartido. |
| **Segundo HITL — emitir** `fact-hitl` | ✅ | ✅ | La card de chat NO repite textualmente la frase de irreversibilidad ('se anula con nota de crédito') que el prototipo pone en el propio hilo — ese texto sólo aparece en el paso 'Resumen' de PantallaFacturacion (flujo manual), no en el gate del chat; confirmar si eso importa para la decisión de diseño de HITL. | apps/copiloto-web/src/modules/chat/TarjetaFacturaPropuesta.tsx:92-146 — card con cliente/CUIT, ítems, total, botón 'Emitir' (o 'Completar a mano' si falta algo) que llama confirmarConTokenFresco (packages/core), token fresco + verificación por relectura de estado, tal como exige el irreversible del prototipo. El aviso de irreversibilidad ('una factura emitida no se borra, se anula con nota de crédito') vive en PantallaFacturacion (paso resumen) para el flujo manual; en la card de chat el aviso corto es 'Esto entendí... tocá Emitir'. | apps/mobile/src/modules/chat/TarjetaFacturaPropuesta.tsx:84-148, idéntico patrón (Row cliente/ítems/total + FilaBotones Emitir/Completar a mano) sobre confirmarConTokenFresco. |
| **Emitida — el CAE** `fact-cae` | 🟡 | 🟡 | Falta portar el comprobante con CAE + acciones Guardar/Mandar por mail/Compartir por WhatsApp AL PROPIO MENSAJE del chat que emitió — hoy el usuario que factura desde el chat no ve el CAE ni puede compartir sin salir a la pantalla de Facturación. Es la brecha real frente al principio del prototipo 'el comprobante queda en la conversación que lo produjo'. | Tras emitir desde el chat, TarjetaFacturaPropuesta.tsx:59-66 sólo muestra el texto terminal 'Factura emitida.' (Surface variant tile) — SIN CAE, SIN PDF, SIN chips de compartir en el hilo. El CAE + 'Guardar'/'Compartir' SÍ existen pero en otra pantalla: apps/copiloto-web/src/modules/facturacion/TarjetaComprobante.tsx:82 ('CAE: {resultado.cae}') y líneas 110-139 (Guardar/Compartir), dentro de PantallaFacturacion, no en el chat. | Mismo patrón: apps/mobile/src/modules/chat/TarjetaFacturaPropuesta.tsx:54 sólo 'Factura emitida.' en el hilo; CAE y compartir viven en apps/mobile/src/modules/facturacion/TarjetaComprobante.tsx (confirmado con grep 'CAE'). |
| **Presupuesto por voz** `pres-voz` | ✅ | ✅ | No se verificó en runtime la secuencia exacta 'qué le cotizás, decime los ítems con su precio' turno por turno — depende del prompt/tools del backend en producción, no de código de UI estático. | El pedido de ítems uno a uno es diálogo de texto plano del LLM (mismo mecanismo que fact-voz); la card resultante TarjetaPresupuestoPropuesto.tsx:91-96 arranca en 'editando' con el texto 'Esto entendí. Revisalo, corregí lo que haga falta y tocá Guardar — todavía no lo anoté', que replica literalmente la copy de pres-hitl/pres-voz del prototipo. | apps/mobile/src/modules/chat/TarjetaPresupuestoPropuesto.tsx — mismo patrón 1:1 (FormularioPresupuesto via iniciales). |
| **La tarjeta editable** `pres-hitl` | ✅ | ✅ | Mobile NO tiene el guard cross-reload contra duplicados que web sí agregó (ver comentario en TarjetaPresupuestoPropuesto.tsx de mobile líneas 17-27 y el de web líneas 22-34): un reload de página en mobile puede re-mostrar la card ya guardada en estado 'editando' y generar un presupuesto duplicado al tocar Guardar de nuevo. | apps/copiloto-web/src/modules/chat/TarjetaPresupuestoPropuesto.tsx:91-121 — card editable fila por fila vía FormularioPresupuesto con botones Guardar/Descartar; 'todavía no lo anoté' textual (línea 95); guard cross-reload contra duplicado documentado en líneas 22-34. | apps/mobile/src/modules/chat/TarjetaPresupuestoPropuesto.tsx:32-45 mismo patrón; docstring del propio archivo (2026-08-12) admite que el guard cross-reload SÍ tiene el mismo gap que web tenía antes de arreglarlo (mobile no lo corrigió). |
| **El ciclo** `pres-ciclo` | 🟡 | 🟡 | Falta la sugerencia proactiva post-aprobación ('¿Te armo la factura?' + chip 'Armá la factura') y el chip 'Mandalo por mail'/link al Doc tras guardar el presupuesto — ambos existen sólo como texto suelto en el prototipo, no como affordance tocable en el código real. Además, DetallePresupuesto.tsx (web, línea 28) documenta que 'no hay botón Aprobar' en la UI de pantalla (aprobado sale gratis de Facturar) — la única vía de aprobar sin facturar es la voz, decisión de diseño no confirmada contra el prototipo. | El ciclo completo anotado→aprobado→factura SÍ está implementado a nivel de comando de voz: apps/copiloto/tool_catalog.py:367,1201-1259 define la tool `marcar_presupuesto` disparada por 'me aprobaron el de la panadería' que mueve el estado y devuelve exactamente la frase del prototipo ('Marqué el presupuesto N de X (importe) como aprobado. Confirmáselo...'). Pero el chip/pregunta de seguimiento del prototipo ("¿Te armo la factura?" + chip 'Armá la factura') NO existe: grep de esa frase en apps/copiloto no devolvió resultados, y TarjetaPresupuestoPropuesto.tsx (web y mobile) al guardar sólo muestra el texto terminal, sin chip 'Mandalo por mail' ni link al Doc. | Mismo backend compartido (apps/copiloto/tool_catalog.py) + mismo gap de UI: TarjetaPresupuestoPropuesto.tsx de mobile (línea ~39-45) sólo emite el texto terminal 'Presupuesto anotado — N° X', sin chip de seguimiento ni Doc. |
| **El límite** `limite` | 🔴 | 🔴 | No existe ningún campo de plan/consumo en el backend (`MeResponse` sin plan, confirmado por el comentario de AccountScreen.tsx) ni ningún mensaje de chat de tope de acciones en ninguna plataforma — grep amplio ('límite','acciones del mes','Profesional','tope de acciones') sin resultados relevantes en ninguno de los dos frontends. **DA:** plan/límites — el propio mockup se autodeclara pantalla de visión; el backend no expone el dato en ninguna plataforma | apps/copiloto-web/src/modules/account/AccountScreen.tsx:106-115: fila estática 'Plan: Profesional' con comentario propio '// TODO backend: plan real desde /me (hoy no hay campo de plan/suscripción en MeResponse)'. | apps/mobile/app/ajustes-mi-plan.tsx:1-22: `PantallaAndamiaje` explícito, mensaje 'Acá vas a ver tu plan actual... Todavía no está conectado', con comentario propio confirmando que falta backend. |
| **Calma, ya conocida** `vacio-visto` | 🔴 | 🔴 | No existe el mecanismo de retiro progresivo de la explicación tras N días distintos (localStorage 'odobi-calma-dias' en el prototipo, index.html:3481-3557): el texto explicativo del vacío es fijo, nunca se retira con el uso. | Grep amplio de 'vacio\|calma' en apps/copiloto-web/src limitado a MidiaScreen.tsx/MidiaScreen.test.tsx (mismo texto fijo de L187-193, sin variantes). No hay lectura/escritura de localStorage ni conteo de días distintos en ningún archivo de midia/. | Mismo resultado en apps/mobile/src (PantallaMiDia.tsx/.test.tsx): sin persistencia local ni lógica de 'días distintos vistos'. |
| **Cómo usar la app** `comousar` | 🟡 | 🟡 | Ninguna plataforma implementa el diseño del mockup (lista de 5 temas cortos que abren el CHAT PRINCIPAL con contexto — Decisión C). Ambas abren en cambio un CHAT PROPIO Y SEPARADO ('como_uso_la_app'), que es exactamente la duplicación que el propio repo dice evitar ('un chat de ayuda propio sería la misma duplicación que sacamos con la solapa Preguntar de Inteligencia'). No hay lista de temas visible en ningún componente (grep de 'Cómo usar'/'tema-ayuda'/'comousar' sin resultados en `src/` de ninguna app). | AccountScreen.tsx:158-166 ('Cómo uso la app' → `onNavegarTab('soporte','como_uso_la_app')`), SoporteScreen.tsx:10-13,31-48 (mismo componente de chat que Soporte técnico, sólo cambia `WELCOME_TEXT`), useChatSoporte.ts:36-47 (dos 'funciones' = dos hilos dentro del mismo mecanismo). | PantallaCuenta.tsx:99-106 ('Cómo uso la app' → ruta `/ajustes-soporte?funcion=como_uso_la_app`), PantallaSoporte.tsx:17-20,37-64 (mismo `MarcoGlass`+`ListaMensajes`+voz que Soporte técnico, sólo cambia `TITULO`). |
| **Soporte técnico** `soporte` | 🟡 | 🟡 | En ninguna de las dos apps hay avatar visual ni distinción de que contesta una persona (MessageList.tsx no renderiza avatar alguno — grep 'avatar\|isotipo' sin resultados); tampoco hay texto estático que declare el tiempo de respuesta ni qué datos técnicos viajan con el ticket ('4 h hábiles', 'Va con esta conversación…') — grep sin resultados en ambos módulos `soporte/`. Esos textos, si existen, sólo pueden venir del agente en runtime (backend) y no son verificables sin ejercitar el chat en vivo. | SoporteScreen.tsx:31-48 (chat con `useChatSoporte`), MiTicketScreen.tsx:46-153 (ticket real vía `GET /soporte/tickets/{id}`, código+estado+mensajes), AccountScreen.tsx:149-157 ('Soporte técnico'). | PantallaSoporte.tsx:64-180 (mismo chat con voz embebida vía `BotonVoz`/`useVozComando`), PantallaCuenta.tsx (entrada 'Soporte técnico'). |
| **Contanos qué tal** `feedback` | 🔴 | 🟡 | Web: la pantalla no existe (backend `/feedback` está vivo y en uso por mobile, pero no hay UI web que lo consuma — AUSENTE real, no sólo de ruteo). Mobile: falta la pregunta guiada '¿Qué le cambiarías?' (hoy es un placeholder genérico) y, sobre todo, la sección 'Lo pediste vos' con pedidos anteriores marcados como escuchados —la 'PIEZA' que el mapa marca como esencial— y la nota de derivación a Soporte para problemas reales; grep de 'Lo pediste vos'/'pedido'/'Soporte técnico' en `PantallaFeedback.tsx` no devuelve nada. | Grep amplio de 'Feedback'/'feedback'/'Contanos'/'enviarFeedback' en `apps/copiloto-web/src` sólo devuelve referencias del panel INTERNO `modules/admin/AdminScreen.tsx:686` (feedback entrante que ve el equipo) y un test de regresión de sesión expirada (`sesionExpirada.test.tsx:4`) que documenta un incidente ocurrido en MOBILE — ningún componente ni ruta de usuario expone esta pantalla en web. | PantallaFeedback.tsx:39-193 — textarea+envío de texto (`enviarFeedback`) y grabación de voz (`enviarFeedbackAudio`), reachable desde 'Mi cuenta' (BETA-1a). |
| **Confirmación** `hitl` | ✅ | 🟡 | En mobile, la confirmación genérica (la que corresponde a este `ver`, distinta de las cards tipadas de factura/gasto/ingreso) es notablemente más pobre que en web: falta identidad visual del servicio (ícono/label), campos PARA/MONTO separados, badge de riesgo y el aviso de 'no se puede deshacer' para acciones irreversibles. | apps/copiloto-web/src/modules/chat/HitlCard.tsx:41-125: header con ícono de servicio real + label (L65-69), campo PARA (L73-78), campo MONTO (L80-88), badge de riesgo (L70), preview WYSIWYG (L92-97), aviso de irreversibilidad con borde de alerta (L99-112), acciones Confirmar/Cancelar (L114-121). Coincide de cerca con el `.hitl` del prototipo (index.html:2630-2639). | apps/mobile/src/modules/chat/ListaMensajes.tsx:38-90 (`TarjetaConfirmacion`, único HITL genérico del sprint según comentario L184): sólo texto plano (`gate.markdown`) + botones Confirmar/Cancelar (L64-90). Sin ícono de servicio, sin campos estructurados PARA/MONTO, sin badge de riesgo, sin aviso de irreversibilidad. |
| **Mi día sin avisos** `vacio` | 🟡 | 🟡 | Este vacío vive en la solapa 'Para hoy' del tablero (que absorbió el rol de la portada de avisos del prototipo, ya que no hay portada separada). Falta la ilustración (la app no usa 'sección vacía sin explicación' con imagen) y la estructura título+cuerpo distintos que tiene el prototipo ('Nada urgente por hoy' + texto). | apps/copiloto-web/src/modules/midia/MidiaScreen.tsx:187-193: texto plano ('Hoy no tenés nada pendiente...') sin ilustración ni título separado del cuerpo. Grep de 'vacio\|ilustracion\|taza\|svg\|background-image' en midia.css no dio resultados. | apps/mobile/src/modules/midia/PantallaMiDia.tsx:227-234: mismo texto plano dentro de un `View` centrado, sin ilustración. |

## Las funciones (6)

| Pantalla (`?ver=`) | Web | Mobile | Qué falta | Evidencia web | Evidencia mobile |
|---|---|---|---|---|---|
| **Gastos** `gastos` | 🟡 | 🟡 | En AMBAS plataformas: el mockup pone el mic DENTRO del peek de Gastos ('la acción principal sigue siendo hablar') y la vía foto como disparador directo desde la función; grep confirma `BotonVoz`/`useVozComando` y cualquier cámara ausentes en `modules/gastos/` de mobile y `MicButton` ausente en `modules/gastos/` de web — voz y foto sólo llegan como card producida desde el chat, nunca desde la propia pantalla. Mobile además no adoptó el bloque negro (usa `Tile` glass) — ver decisión abierta. **DA:** Unificación de gramática visual web/mobile (bloque negro Monzo vs. sistema glass) — divergencia documentada, no resuelta a la fecha del diff hermano 2026-09-08. | GastosScreen.tsx:82-180 (stack+bloque+alta manual), ResumenMes.tsx:17-54 (total+desglose por categoría con %+comparación mes anterior, Surface variant="bloque" gramática Monzo), TarjetaGasto.tsx:15 (ícono por `origen`), FormularioGasto.tsx:100-111 (cita OCR 'Del ticket leímos…' para origen 'foto'). | PantallaGastos.tsx:130-221 (mismo flujo: resumen+FilaBotones 'Anotar un gasto'+lista+BuscadorActividad), ResumenMes.tsx:26-81 (mismos datos pero en `Tile` glass, no bloque negro), FormularioGasto.tsx:51,126-140 (origen 'manual'\|'voz'\|'foto'). |
| **Ingresos** `ingresos` | ✅ | 🟡 | Mobile no tiene el bloque de resumen mensual: sólo muestra `total` crudo (PantallaIngresos.tsx:145-152), sin la etiqueta 'Cobraste este mes' ni comparación 'Mes anterior' que sí tiene web y pide el mockup (`#ingresos .resumen .comp`); tampoco tiene el aviso 'Los cobros por MercadoPago todavía no entran solos' que web sí porta. Mismo gap de mic/foto embebidos que Gastos, en ambas plataformas. | IngresosScreen.tsx:92-196 (stack+bloque+alta+lista+aviso MercadoPago verbatim), ResumenIngresos.tsx:20-36 ('Cobraste este mes'+total+'Mes anterior: $X', calco literal del mockup `#ingresos .resumen`). | PantallaIngresos.tsx:100-227 (listado con `origen` por fila vía ETIQUETA_ORIGEN:39-44, alta manual 'Anotar que me pagaron':158-164, mismo aviso MercadoPago no está presente — ver falta). |
| **Facturación** `factura` | ✅ | ✅ | No se renderizó visualmente para confirmar paridad exacta de estilo/chip de estado (impago en negro vs. paleta semántica) — el código lo declara pero no se verificó en pantalla real. | apps/copiloto-web/src/modules/facturacion/ResumenFacturacion.tsx (bloque negro 'Facturado este mes' + total + comp, con regla dura '—' si no se sabe, nunca $0) + apps/copiloto-web/src/modules/facturacion/SeccionMisComprobantes.tsx (listado de emitidas con estado) + apps/copiloto-web/src/modules/facturacion/SeccionMeDeben.tsx (impagas), todo dentro de PantallaFacturacion.tsx — replica 1:1 la anatomía del bloque #factura del prototipo (index.html:1938-1975). | apps/mobile/app/facturacion.tsx + apps/mobile/src/modules/facturacion/PantallaFacturacion.tsx, SeccionMisComprobantes.tsx, TarjetaComprobante.tsx confirmados por grep 'CAE' — misma estructura que web (docstring de web dice explícitamente que es el port de mobile). |
| **Presupuestos** `presu` | ✅ | ✅ | No se renderizó visualmente para confirmar layout final; funcionalmente cubierto por código leído. | apps/copiloto-web/src/modules/presupuestos/PresupuestosScreen.tsx:64-82 — docstring cita explícitamente el mockup fuente (#presu) y replica la regla 'lo que sigue esperando respuesta' (no lo presupuestado) calculado sobre vigentes con estado==='pendiente'; TarjetaPresupuesto.tsx maneja los chips de estado (Aceptado/Se enfría/Enviado). | apps/mobile/app/presupuestos.tsx + apps/mobile/src/modules/presupuestos/ (misma estructura, TarjetaPresupuesto.tsx con estados). |
| **Inteligencia de Negocio** `bi` | 🟡 | 🟡 | En AMBAS plataformas falta el bloque 'Acumulado del año — Facturado en los últimos 12 meses' con el medidor de tope de monotributo (mockup `#inteligencia` líneas ~1908-1917): grep de 'tope'/'monotributo'/'Facturado en los'/'acumulado' en ambos módulos de inteligencia no devuelve nada; el propio comentario de InteligenciaScreen.tsx:278-283 declara el gap explícito y escalado a coordinación como hallazgo abierto (backend no expone el agregado ni tokens de semáforo). | InteligenciaScreen.tsx:105-292 — bloque 'Saldo en caja'+Entró/Salió (180-196), grilla 4 KPIs+Rentabilidad con nota '—' (198-238), Por cobrar+vencido (240-252), Mejores clientes (254-276), GraficosInteligencia (Entró vs salió, En qué se te va la plata, Margen por trabajo — GraficosInteligencia.tsx:138-191). | PantallaInteligencia.tsx:120-246 — mismos bloques (caja, mes, por cobrar, serie mensual con barras propias, mejores clientes, GraficosInteligencia con Margen por trabajo en graficos/GraficosInteligencia.tsx:150-210). |
| **Clientes** `clientes` | ✅ | 🟡 | Mobile no tiene el bloque 'Le vendiste a N clientes' ni el chip 'se agregaron solos este mes': grep de 'Le vendiste a'/'agregaron solos'/'cli-n'/'carteraBase' en `PantallaClientes.tsx` sólo devuelve el texto del vacío ('Tu cartera se va a armar sola…'), no un resumen numérico. Es justamente el elemento que el propio mapa marca esencial ('si el usuario no sabe que la cartera se arma sola, ver nombres que no cargó se lee como error'). | ClientesScreen.tsx:228-245 — bloque negro 'Le vendiste a'+cifra+chip 'N se agregaron solos este mes' (calco de mockup `.cli-n`), búsqueda con debounce (79-116), pill 'Nuevo cliente' (277-290), lista + aviso de duplicado (247-260). | PantallaClientes.tsx:74-320 — listado, búsqueda con debounce (90-115), alta manual 'Nuevo cliente' (257-267), ficha, aviso de duplicado; SIN el bloque resumen. |

## Ajustes y sus opciones (8)

| Pantalla (`?ver=`) | Web | Mobile | Qué falta | Evidencia web | Evidencia mobile |
|---|---|---|---|---|---|
| **Ajustes** `ajustes` | ✅ | ✅ | Se entra por un tile del grid de Funciones (EscritorioFunciones.tsx:126 en mobile; tab fijo `ajustes` en el Rail/TabBar en web), no por el avatar como única puerta que fija la Decisión A del prototipo (mockup 13-ajustes §1). Ligado a la decisión abierta de arquitectura de navegación (capas/avatar vs tabs, mockup 10-arranque). | apps/copiloto-web/src/modules/ajustes/PantallaAjustes.tsx:16-40 define las mismas 7 keys y mismo orden que el prototipo (Mi negocio, Facturación AFIP/ARCA, Apps conectadas, Mi plan, Mi cuenta, Apariencia, Cómo hablarle); apps/copiloto-web/src/modules/ajustes/AjustesScreen.tsx:29-72 orquesta la navegación local. Gramática B respetada: header simple (h1) sin bloque de color (PantallaAjustes.tsx:50-53), sin cifra de negocio. | apps/mobile/src/modules/ajustes/PantallaAjustes.tsx:14-82 (TILES_AJUSTES) mismas 7 entradas, mismo orden, grilla 3 columnas. MarcoGlass sin bloque negro. |
| **Mi negocio** `negocio` | 🟡 | 🟡 | Faltan los campos Teléfono y Email que el prototipo muestra en Mi negocio (index.html:2200-2203) — ausencia confirmada también en el contrato de datos, no sólo en la UI (grep en `perfilNegocio.ts` sin 'telefono'/'email'). Además la resolución del duplicado 'Cómo hablarle' que fija DECISIONES.md §2 (fila-resumen que linkea a una pantalla propia de tono) no se implementó así: acá está el editor completo y 'Cómo hablarle' pasó a ser otra cosa (ver fila 'hablar'). | apps/copiloto-web/src/modules/ajustes/negocio/PantallaPerfilNegocio.tsx:48-58 (Campos) y :318-444 sólo tiene queVende/aQuien/nombreComercial/horarioAtencion + sección tono/largo/nombre (duplicada, no fila-resumen). packages/core/src/api/perfilNegocio.ts:40-60 (interface PerfilNegocio) confirma que el contrato NO tiene teléfono ni email. | apps/mobile/src/modules/ajustes/negocio/PantallaPerfilNegocio.tsx:75-86 (Campos) mismos 4 campos + tono/largo/nombre en sección propia (:409-444), sin teléfono ni email. |
| **Facturación ARCA** `afip` | ✅ | ✅ | Ninguno esencial detectado por código estático; el mensaje textual exacto 'la clave fiscal no se guarda' y el flujo de vinculación de varios minutos (que DECISIONES.md §4 marca como no dibujado ni en el propio mockup) sólo se puede confirmar renderizando/en device. | apps/copiloto-web/src/modules/ajustes/afip/PantallaAfipSetup.tsx:386-472 (CUIT, Razón social, Domicilio, Condición IVA, Ingresos brutos, Inicio de actividades, Punto de venta) + bloque Ambiente (:59-355, ETIQUETA_AMBIENTE Testing/Producción) + CUIT bloqueado con escape 'Cambiar' (:370-386), equivalente al 'Necesito cambiarlo ›' del prototipo (index.html:2223-2225). | apps/mobile/src/modules/ajustes/afip/PantallaAfipSetup.tsx:560-608 mismos 6 campos + CUIT bloqueado con botón 'Cambiar' (:531-553) + Usuario ARCA/Clave fiscal (:787-794) + Ambiente. |
| **Apps conectadas** `apps` | 🟡 | ✅ | Web: falta la descripción/capacidad bajo cada app (el prototipo la exige explícitamente — index.html:2250 'Mandar correos por vos. No puede leerlos.') y no existe acción de desconectar, aunque el prototipo mismo (mockup 13-ajustes §4) tampoco la dibuja, así que ese punto es evidencia adicional, no el criterio del veredicto. Mobile: sin falta esencial detectada. | apps/copiloto-web/src/modules/connections/ConnectionsScreen.tsx (grid de servicios, connect real vía OAuth :50-60) + ServiceCard.tsx:55-92: la card sólo pinta `service.display_name` y el estado (CONECTADO/Conectar) — no hay ningún nodo para `description`/capacidad, ni acción de desconectar (sólo 'connected'\|'reconnect'\|'disconnected' derivados de un booleano, ServiceCard.tsx:30-35). | apps/mobile/src/modules/apps/PantallaApps.tsx:240-343: cada fila muestra nombre + `s.descripcion` (nombradas por capacidad, igual al prototipo) y tiene flujo completo de Conectar/Desconectar con aviso real de 'lo que se pierde' (`loQueSePierde`, :68-74, y `CONSECUENCIA_EXTRA` :89-93 para Drive/MercadoPago). |
| **Mi plan** `plan` | 🟡 | 🟡 | El backend no expone plan/consumo (confirmado por el propio código: ambas plataformas usan el placeholder honesto PantallaAndamiaje en vez de datos). Faltan el medidor '64 de 200 acciones', la explicación Gasta/No gasta y el aviso de tope que pide el prototipo (index.html:2265-2271). No es un bug de UI sino ausencia de dato/endpoint — DECISIONES.md §4 ya lo marcaba como 'visión' el 19/08. | apps/copiloto-web/src/modules/ajustes/AjustesScreen.tsx:63-69 renderiza PantallaAndamiaje con mensaje 'Todavía no hay planes disponibles para elegir'. | apps/mobile/app/ajustes-mi-plan.tsx:13-22 renderiza PantallaAndamiaje con mensaje 'Acá vas a ver tu plan actual... Todavía no está conectado' (icono miPlan, ruta cableada). |
| **Mi cuenta** `cuenta` | 🟡 | 🟡 | Faltan en ambas plataformas las dos filas que el prototipo define como el contenido central de Mi cuenta —'Cambiar el mail' y 'Cambiar la contraseña' (index.html:2280-2283, bajada del mapa: 'Mail, contraseña y cerrar sesión')—; en su lugar hay filas no previstas por el prototipo (Plan/Idioma/Notificaciones en web, Feedback en mobile) que vienen de una fusión con AccountScreen/PantallaCuenta previa, no del mockup 13. | apps/copiloto-web/src/modules/account/AccountScreen.tsx:88-224: identidad real (email de /me, :12-16), Plan/Idioma/Notificaciones (placeholders declarados como TODO backend, :107-126), No molestar inerte con explicación (:127-138), Soporte técnico/Cómo uso la app/Privacidad del historial, Cerrar sesión con confirmación (:171-206). Grep de 'contrase' sólo encuentra el texto del diálogo de logout — no hay fila 'Cambiar el mail' ni 'Cambiar la contraseña'. | apps/mobile/src/modules/ajustes/PantallaCuenta.tsx:30-175: mismo patrón (email real :43-50, Feedback, Soporte técnico, Cómo uso la app, No molestar inerte, Cerrar sesión con confirmación :132-171). Mismo grep sin resultado de cambio de mail/contraseña. |
| **Apariencia** `apar` | 🟡 | 🟡 | El prototipo pide 'Dos temas exactos' con muestras reales y una fila 'Como el teléfono' que sigue al sistema (index.html:2311-2318); ambas apps implementaron 3 temas en vez de 2 y ninguna tiene el seguimiento automático del sistema operativo. Web además no muestra las muestras de color reales que sí tiene mobile. Ligado a la decisión abierta de tokens/tipografía (paleta 'nocturno' no contemplada en el mockup 13). | apps/copiloto-web/src/modules/ajustes/PantallaApariencia.tsx:7-11,20-46: THEME_LABELS tiene 3 temas (claro/oscuro/nocturno) como píldoras con aria-pressed, sin muestras de color (fondo/texto/acento) y sin opción 'Como el teléfono'. | apps/mobile/src/modules/ajustes/PantallaSkins.tsx:9-19,33-90 (ETIQUETA_SKIN/ORDEN_SKINS) también 3 skins, pero SÍ con muestras de color reales por skin (`muestrasDe`, :15-17, 4 chips por card) — más fiel al prototipo que web en ese punto. Tampoco tiene 'Como el teléfono'. |
| **Cómo hablarle** `hablar` | 🟡 | 🟡 | El prototipo define 'Cómo hablarle' como el editor de Tono/Largo/Nombre con una respuesta de ejemplo que cambia según la elección (index.html:2323-2343, bajada del mapa: 'Tono, largo y el nombre'). Ambas apps repropusieron el mismo nombre de pantalla para una funcionalidad distinta (guía de capacidades del copiloto); el editor de tono real vive duplicado dentro de 'Mi negocio' (ver fila 'negocio'), no en una pantalla propia como pide DECISIONES.md §2 del mockup 13. Es una divergencia de producto, no un detalle visual. | apps/copiloto-web/src/modules/ajustes/PantallaComoHablarle.tsx:8-104: la pantalla es una GUÍA DE CAPACIDADES ('qué se le puede pedir al copiloto', ejemplos desde GET /capacidades, :9-14) — no un editor de Tono/Largo/Nombre ni muestra una respuesta de ejemplo configurable. | apps/mobile/src/modules/ajustes/PantallaComoHablarle.tsx:9-34: mismo contenido — guía de ejemplos por capacidad viva, explícitamente distinto de un editor de tono ('Los ejemplos NO están escritos acá... vienen de GET /capacidades'). |

## Límites de esta medición

- Estático: lo visual/táctil (animaciones, gestos, contraste) no se confirma sin renderizar; donde importa, está dicho en «Qué falta».
- No se implementó nada. Qué construir y en qué orden es decisión del operador; las filas **DA** además esperan la reunión con Martin (contrato 2026-09-16 Parte 2).
