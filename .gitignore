# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Secretos — nunca al repo (los .template SÍ se versionan)
.env
.env.*
!.env*.template
Openai apikeyp grafity backup 341.txt
afip sdk tincho toc.txt
Usuario y clave fiscal David Huck Afip.txt


# Frontend
node_modules/
apps/copiloto-web/dist/
*.tsbuildinfo
.vite/

# Assets pesados (diseño / spike de voz) → almacenamiento externo, ver docs/ASSETS-EXTERNAL.md
*.zip
es-ar-listen/
**/APP Copiloto Movil/
**/Web copiloto/

# Buzón entre sesiones — NO es parte del repo.
# Carpeta física única apuntada por los crones vía ruta absoluta. Si se versionara,
# `git worktree add` la duplicaría por worktree y el mensaje de una sesión no existiría
# para la otra. Ver docs/superpowers/specs/2026-07-21-formato-coordinacion-tres-sesiones-design.md
coordinacion/

# OS / editor
.DS_Store
Thumbs.db

# Staging del clon documed (extraído por scripts/sprint-mobile/S1) — NO se versiona
_staging/

# PII / evidencia / basura estructural — NUNCA al repo (riesgo real de `git add -A` en checkout
# compartido por 3 sesiones). Factura/ = fotos de comprobantes (PII); _evidencia/ = capturas de
# device incl. pantallas de login; code/ = clon de OTRO repo del workspace copiado por error.
Factura/
_evidencia/
code/

# Mobile
apps/mobile/.expo/
apps/mobile/android/
apps/mobile/ios/
*.apk

# Estado local de Claude Code (no versionar; los commands SÍ van)
.claude/settings.local.json
.claude/scheduled_tasks.lock
