---
name: kit-canonico-skeleton
description: "Kit canónico del skeleton (sub-proyecto 1 de SP7) — plantillas por arquetipo + manual + scaffold + validate, cero-duplicación, validado 5/5 gates reales"
metadata: 
  node_type: memory
  type: project
  originSessionId: 93554263-af57-48fd-badc-53bcc5a53d6b
---

**2026-06-22, ✅ MERGEADO a `main` PR #54 (squash; rama `feat/skeleton-kit-canonico`, commit `887244d`).** Sub-proyecto 1 de **SP7** (el paso previo que el operador pidió antes de la orquestación durable). Kit de capa-plantilla en `deploy/skeleton_kit/` que estandariza cómo se escribe un esqueleto **flujo C** canónico — el "mapa de construcción". Sirve a dos consumidores: el operador escribiendo el plano en VS Code hoy, y la automatización full E2E futura (sub-proyecto 2) como contrato a seguir. Destila el `uc-stack-real` ya validado E2E (spike-first ya satisfecho — canoniza un patrón que pasó).

**Forma = kit** (decisión del operador): `MANUAL.md` (mapa de construcción autónomo + ref al reporte stack canónico) · `archetypes/<name>/{stub,test_stub,reference_impl}.py + README.md` (5 arquetipos) · `reference/README.md` (puntero) · `scaffold.py` (copy+rename word-boundary; NUNCA copia reference_impl/README) · `validate_kit.py` (done-por-test) · `tests/{test_consistency,test_scaffold}.py`.

**5 arquetipos validados E2E** (sin especulativos): `backend_temporal` (cage temporal: workflow durable + activity, store inyectado, `workflow.sleep`) · `client_sdk_injected` (python: SDK por DI + fake) · `client_http_plain` (python: urllib + contrato HTTP fiel, header case-INsensitive) · `frontend_render` (browser: render() done-by-test, data-testid) · `pure_logic` (python: lógica pura). Los 4 del stack salen byte-exactos de `gen_skeleton_uc_stack_real.FILES` + `_validate_skeleton_gates.FILLS`; pure_logic es nuevo.

**Cero-duplicación (decisión pre-flight, alineada con [[self-test-kit]]/factory_kit):** los archetypes son la **fuente única**; `gen_skeleton_uc_stack_real.py` se refactorizó (DRY) para CONSUMIR los archetypes (370 líneas inline → 14) preservando el output byte-idéntico (golden `6f2fe6f`, characterization spike-first antes del refactor). `reference/` es puntero (+ desde PR #57 el molde `validate_reference.py`, ver cierre — NO es duplicación de output-de-generador: el validate.py no tiene generador, se escribe a mano por app). `_validate_skeleton_gates.py` deprecado → shim a validate_kit.

**Validación (evidencia ejecutable):** `validate_kit.py` en el VPS → **KIT_PASSABLE: YES (5/5)** — cada arquetipo pasa el gate Docker REAL de su cage (reusa `feature_activities._gate_for_cage` + `_patch_test_imports` + `factory_kit.cage_for_path`). + golden 2/2, scaffold 5/5, pure_logic 4/4. Review final opus **READY-TO-MERGE** (0 crit/high/med). Frontera (regla 6) y determinismo (regla 3) intactos. Tests con gate Docker SOLO en el VPS ([[tests-se-corren-en-vps]]).

**Ejecución:** brainstorming → spec → writing-plans → subagent-driven (NO commits por-task, UN commit final por pedido del operador). Pre-flight cazó duplicación verbatim archetype↔reference → reestructurado a cero-duplicación ANTES de codear.

**Molde del `validate.py` ✅ AGREGADO (PR #57 MERGEADO `240e6f9`, rama `feat/kit-validate-py-archetype`, 2026-06-22).** Cierra el último gap del kit: los stubs+tests ya tenían arquetipo, el `validate.py` (validación POST-gate que corre `validate_real`, frontera A) se escribía a mano sin molde. Pieza en `reference/validate_reference.py` = el `validate.py` REAL de `uc-stack-real` (cuerpo **byte-idéntico**, pasó las 4 plataformas vivas) + header de procedencia; hogar `reference/` (NO `archetypes/`: es repo-level + post-gate + **NO gate-validable** como los 5 triads → sin falsa simetría, lección [[self-test-kit]]). Guía de adaptación en `reference/README.md` §validate.py + `MANUAL.md` §11 (harness `R`/`_p`+exit-code · layout real no-flat · SDK real por inyección creds-por-env · escenario cross-capa · gotcha del **val-venv** `/opt/uc-val-venv`: un SDK nuevo va instalado ahí o `validate_real` revienta `ImportError` post-gate). Smoke **honesto**: `validate_kit.py` solo `py_compile` (correrlo en vivo necesita instancias reales = la frontera A). Verificación adversarial 3 lentes opus: 0 crit/0 imp/2 minor (fixeados: línea `Run:` stale + gotcha val-venv); gate VPS 5/5 + suite 9 passed PC+VPS. **Con esto el "plano" del flujo C queda COMPLETO: `skeleton/` (stubs+tests) + `validate.py` (post-gate).** Gap pre-existente cerrado de paso: `scaffold.py` deployado al VPS (era helper PC-side no deployado → la suite ahora colecciona también en el VPS).

**SP7 COMPLETO:** sub-proyecto 1 kit (PR #54) + sub-proyecto 2 [[seniorworkflow-durable-sp7]] (PR #55) + este molde (PR #57). Spec/plan del kit en `docs/superpowers/{specs,plans}/2026-06-22-kit-canonico-skeleton*`. Reporte: `docs/Implementaciones terminadas/2026-06-22-molde-validate-py_reporte.md`.
