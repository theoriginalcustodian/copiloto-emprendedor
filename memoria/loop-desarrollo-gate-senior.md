---
name: loop-desarrollo-gate-senior
description: Loop de desarrollo (gate senior Claude headless autónomo + corrección iterativa) + app real unitkit E2E (PR real) + nivel 3 cascade = /goal auto-convergente durable en Temporal
metadata: 
  node_type: memory
  type: project
  originSessionId: 93554263-af57-48fd-badc-53bcc5a53d6b
---

**2026-06-21 — 3 hitos E2E encadenados (dirigido por `/goal "terminar todo"`, autónomo). ✅ MERGEADO a main PR #52 (`aed3756`, squash, 2026-06-22). Rama `feat/loop-desarrollo-gate-senior` (borrada en remote).**

**(1) LOOP DE DESARROLLO (gate senior + corrección).** `judge_unit` (activity, `feature_activities.py`): Claude headless **LEE el código como dato** (`Read Glob Grep`, nunca ejecuta — frontera regla 6) y evalúa CALIDAD tras pasar los tests (caza reward-hacking que el gate no ve). Cascade de jueces **unificada con la de relleno**: flash→**haiku**, pro→**sonnet**, claude→**opus** (`_DEFAULT_JUDGE_MAP`, piso haiku ~$0.067/juicio = routing de /goal). `_judge_or_escalate` = control-flow replay-safe: reject no-terminal → `reason` en `last_out` = feedback localizado al sig nivel ("dale el plano"); reject terminal → `ACCEPTED_WITH_DEBT`. **`gate_agent.py`** (`deploy/ops/`) = **gate senior AUTÓNOMO**: Claude headless poolea el estado del workflow → juzga el repo → firma el signal `decide` (reemplaza al operador en gate1+gate2). Review ultracode cazó **5 findings de raíz** (el central: el `reason` NO se propagaba al fill flash/pro → el loop escalaba CIEGO). E2E: judge_unit Haiku approve $0.0707, reasoning genuino.

**(2) ROADMAP 1 — APP REAL `unitkit` (100% autónoma).** Conversión de unidades, 4 módulos planos (dimensions ← convert, parse, format_qty), **flujo C** (operador andamia skeleton, fábrica rellena). `create_project.py` → repo GitHub privado real + clone + grafo Graphity. gate-agent (Claude) firmó gate1+gate2 con reasoning de calidad ("proving generality over test-fitting"). **memory_enabled=True → Graphity ingirió 13 nodos** (8 knowledge + 5 structural, failed:0; retrieve vacío en la 1ª feature = correcto). **PR #1 MERGED** (squash + rama borrada). **18 tests verdes en clone fresco** (evidencia honesta). Costo $0.012 flash + $0.16 jueces Haiku + $0.97 gates Claude sombra Max. **Gotcha:** `skeleton/` untracked deja 2 `test_X.py` (root+skeleton) → `pytest` desde el working dir da `import file mismatch` → NO es bug (PR limpio, skeleton/ gitignored) → verificar con **clone fresco**.

**(3) ROADMAP 2 — nivel 3 cascade = /goal AUTO-CONVERGENTE.** `/goal` headless LITERAL con auto-verificación de tests **VIOLA la frontera** (Claude correría pytest sobre código IA en el VPS) → implementado como **bucle DURABLE en Temporal** (operador: *"excelente decisión implementar en temporal, esa era la idea"*). `goal_max_turns = max(1, int(req.get("goal_max_turns") or 1))`: **default 1 = SP5 exacto** (backward-compat), >1 = auto-convergente **activable por métrica** (el director lo sube p/ units difíciles). Bucle nivel 3: `claude_fill_unit` (Claude **escribe**) → `_docker_gate` (sandbox `--network none` **ejecuta** — frontera intacta) → si rojo, el `output` del gate vuelve como feedback; si verde, `judge_unit` (terminal solo en el último turno) → approve resuelve / reject no-terminal = feedback p/ reintentar / reject terminal = debt. Es **/goal en espíritu** (evaluador estructurado + reason-feedback + auto-convergencia + tope) **con la durabilidad cross-corte que /goal efímero no tiene** (doc research §20.5). 178 passed (5 tests `test_goal_loop_*`: converge-tras-rojo, converge-tras-reject, agota→debt, default=SP5, rojo→human) + **review adversarial inline** (workflow ultracode cayó por 529 → review por sesión principal, 5 dim OK, 1 hallazgo menor `int(None)` fixeado). E2E no-regresión (unit difícil `roman`, max_iters=1): **flash resolvió en L1** → el nivel 3 (airbag raro) NO se ejercitó E2E — el músculo es competente, forzarlo con test imposible NO sirve (el gate-agent lo rechaza en gate1); los unit tests deterministas cubren ese control-flow (decisión honesta: no codificar la esperanza con un caso de juguete).

**INCIDENTE Anthropic 529 (lección de diseño).** Sobrecarga server-side ~20min tumbó el workflow ultracode de review (2×) + el **classifier de seguridad de Bash** (bloqueó lanzar el E2E). Observación del operador: *"por cosas como estas es mejor correr trabajos en Temporal"* — la **tesis del proyecto validada contra un corte de Anthropic** (no solo Kaggle): el **plano de control efímero** (sesión Claude Code + classifier) es el eslabón frágil; un **workflow encolado en Temporal es inmune** (activities Claude con retry esperan durablemente el 529; DeepSeek/OpenRouter ni depende de Anthropic). → **justifica empíricamente SP7** (intake de Hermes desde el VPS, sin el orquestador interactivo). El intake debe ser tan durable como el trabajo.

Reporte `docs/Implementaciones terminadas/2026-06-21-loop-desarrollo-app-real-goal-headless_reporte.md`. Specs/plans `docs/superpowers/{specs,plans}/2026-06-21-loop-de-desarrollo*`. Spikes `spikes/{autonomous-gate,dev-loop,roadmap1-unitkit,roadmap2-goal-loop}/`. Relacionadas: [[casa-fabrica-features-diseno]] · [[memoria-grafo-fabrica-diseno]] · [[loop-engineering-framing]] · [[spike-first-central-proyecto]] · [[claude-code-headless-capabilities]].
