---
name: copiloto-gotrue-dedicada-cutover
description: "Copiloto migrado a una GoTrue DEDICADA (Opción C, aislada de fusion) — cutover EJECUTADO en vivo. LEER al tocar auth/login/signup del copiloto, al activar Google OAuth, o al operar/rollbackear el stack de auth."
metadata:
  node_type: memory
  type: project
  originSessionId: 53c58a53-271e-474c-9267-f7720a2c6820
---

**El copiloto ya NO usa la GoTrue compartida de fusion: tiene su GoTrue DEDICADA** (secreto+issuer propios), en el VPS del copiloto, loopback `127.0.0.1:9997`. **Cutover EJECUTADO y verificado EN VIVO** (2026-07-05, **PR #130 MERGED** a main). Motivo: cerró el **SSO-by-accident** — `require_tenant` no validaba `iss`, así que un token de CUALQUIER app de fusion firmado con el `SUPABASE_JWT_SECRET` compartido entraba al copiloto (verificado por el workflow adversarial de auth-eval). [[copiloto-deploy-multitenant-vivo]]

**Review adversarial (ultracode, 3 lentes) → 12 findings verificados; TODOS los fixables aplicados + verificados EN VIVO** (2 alta / 3 media / +baja, 2º commit del PR): A1 mapeo old→new persistido (rollback-DB; backfilleado retroactivo `copiloto-gotrue-migration-map.tsv` 600) · A2 `deploy.sh [2/7]` gateado por marker `.gotrue-cutover-done` (no repointa a ciegas pre-cutover/DR) · M3 creds append-only por fila (crash≠lockout) · M4 iss fail-closed vía flag `COPILOTO_DEDICATED_GOTRUE=true` (serve.py aborta si falta issuer con el flag; el flag NO se setea en rollback→no rompe volver a fusion) · M5 vhost público restringido a paths OAuth (deny `/admin/*`) · B7+B10 `repoint-env.sh` fuente única con escaping · B9 healthcheck BusyBox-wget en auth. **Diferido (deuda registrada):** B6 (ventana de 403 selectivos escala mal con N tenants grande) · B8 (Google OAuth = registro ABIERTO sin allowlist → decisión de POLÍTICA del operador antes de activar; documentado en el README).

**Topología (todo en el VPS del copiloto, NO en fusion):** `uc-copiloto-web` → `127.0.0.1:9997` (Caddy proxy `/auth/v1/*`) → `gotrue:9999` (supabase/gotrue **v2.186.0**, misma que fusion → contrato de token idéntico) → `postgres:16-alpine` (schema `auth`). Fusion = **VPS APARTE** (alias SSH `fusion` desde el VPS del copiloto); la **data** del copiloto (`uc_factory.tenants`, `mp_credentials`) SIGUE en la Postgres de fusion (`fusion-pg.env` = `DATABASE_URL`, no se tocó). El cutover solo cambió QUIÉN emite/valida tokens.

**Código (todo en `deploy/copiloto/gotrue/` + apps/copiloto):** compose parametrizado (`docker-compose.gotrue.yml`, cero hardcoding, env `copiloto-gotrue.env` server-side 600) · `deploy-gotrue.sh` (idempotente: genera secreto+keys la 1ª vez, los preserva; auto-valida contra el stack real) · `migrate_tenants.py` + `migrate-and-cutover.sh` (migra tenants + repoint + restart + smoke + rollback por backup) · README con la guía de activación de Google. Reuso: es un **drop-in** de `SUPABASE_URL` (el proxy mapea `/auth/v1` → GoTrue, el código del copiloto no cambia salvo iss).

**REGLAS (no romper):**
- `decode_supabase_jwt(..., issuer=X)` valida `iss`; `issuer=None` = legacy sin verificar (backward-compat). El composition root inyecta `COPILOTO_JWT_ISSUER` → `require_tenant` valida iss. **Nunca** usar el `SUPABASE_JWT_SECRET` de fusion para el copiloto: es `COPILOTO_JWT_SECRET` propio.
- `deploy.sh [2/7]` re-proyecta `copiloto-gotrue.env` → EnvironmentFiles en CADA deploy (self-healing). NO volver a sourcear fusion para el JWT.
- **Rollback** = restaurar `copiloto.env` + `fusion-supabase.env` de los `*.bak-pre-gotrue-<stamp>` + `systemctl restart uc-copiloto-web uc-copiloto-worker` (los passwords viejos de fusion vuelven a andar).

**Deuda gestionada:** los 4 tenants migrados tienen **passwords temporales** en `/etc/unreal-copilot/copiloto-migrated-creds.txt` (600) — no se migran hashes entre instancias sin tocar fusion. Reset sin SMTP → **OTP por WhatsApp** (canal ya operativo), diferido. · service_role/anon keys exp 10y sin revocación server-side.

**Google OAuth: ✅ LIVE + PROBADO E2E EN PRODUCCIÓN (2026-07-05, PR #132 MERGED).** El operador entró con Google y se verificó contra la base que **vinculó a su cuenta existente** (mismo `sub` migrado `2598d84b…`, 1 sola fila de tenant, cliente_id preservado — NO creó duplicado; GoTrue linkea por email confirmado). Piezas:
- **Activación idempotente** `deploy/copiloto/gotrue/activate-google-oauth.sh` (creds server-side 600 + vhost + recrea auth + verifica). Creds Google en `copiloto-gotrue.env`; `GOTRUE_EXTERNAL_GOOGLE_ENABLED=true`, `GOTRUE_DISABLE_SIGNUP=false` (necesario para crear el user OAuth; el `/signup` email NO se expone porque el vhost solo proxya /authorize+/callback).
- **Vhost público OAuth-only** `auth.178-105-191-1.sslip.io` (host-level Caddy): expone **SOLO** `/auth/v1/authorize*` + `/auth/v1/callback*` con bloques **`handle`** → todo lo demás (incl. `/admin/*`) da 404. Admin API sigue loopback-only. **GOTCHA cazado en vivo:** un `respond 404` suelto tiene MAYOR prioridad de directiva que `reverse_proxy` en Caddy → tapa TODO el vhost; hay que usar `handle` (mutuamente excluyentes). Y el `umask 077` del script se filtraba al Caddyfile (600 root → `caddy` no lo lee en reload aunque `validate` pase) → preservar perms originales.
- **`API_EXTERNAL_URL` decoplado** del loopback vía `COPILOTO_API_EXTERNAL_URL` (compose; default = loopback). NO afecta el `iss` (fijado por `GOTRUE_JWT_ISSUER`) → iss-enforcement intacto.
- **Frontend** (`apps/copiloto-web`): `auth/oauth.ts` (`googleAuthUrl()` feature-flag por `VITE_AUTH_URL` + `consumeOauthCallback()` captura tokens del fragment + limpia hash) · `SessionProvider` provisiona en el callback (`api.ensureOauthTenant()`, idempotente) · botón en `LoginScreen` · `sync-web.sh` hornea `VITE_AUTH_URL` (derivado de `UC_BASE_DOMAIN`) al build. 429 tests verdes.
- **Modo Testing de Google = allowlist sin código** (solo los *test users* entran) → cubre la decisión de política B8 para arrancar. **Rollback:** sacar vhost del Caddyfile + `GOTRUE_EXTERNAL_GOOGLE_ENABLED=false` + redeploy. [[deuda-secretos-rotar]]

**Evidencia:** spike `spikes/copiloto-dedicated-gotrue/RESULT.md` (GREEN 7/7) · cutover E2E vivo (login tenant migrado → /me 200) · iss-enforcement adversarial EN VIVO (otro emisor → 401, ok → 200, no-tenant → 403) · suite 260 passed/36 skipped. Tests corren en el VPS (`/opt/uc-copiloto-venv`, `cd apps/copiloto && pytest`). [[tests-se-corren-en-vps]] [[deploy-factory-code-vps]]
