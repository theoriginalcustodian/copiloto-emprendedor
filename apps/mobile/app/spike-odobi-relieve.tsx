/**
 * Ruta del spike ODOBI hito 0 (ver `spikes/odobi-relieve/PantallaSpikeRelieve.tsx`). Archivo
 * DESECHABLE — se borra junto con `spikes/odobi-relieve/` al cerrar el contrato. Navegar acá
 * manualmente vía deep link (`adb shell am start -a android.intent.action.VIEW -d "copiloto://spike-odobi-relieve"`)
 * o Metro dev menu; no hay entrada en el escritorio, es sólo para sacar la captura de comparación.
 */
export { default } from '../spikes/odobi-relieve/PantallaSpikeRelieve';
